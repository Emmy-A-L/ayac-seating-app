export type YouthData = {
  _id?: number;
  fullName: string;
  phone?: string;
  register?: boolean;
  transportInfo?: string;
}

export type User = {
    fullName?: string;
    email: string;
    password: string;
}